# Next.js Environment Variables

# Default Theme (available themes: default, ocean, sunset, cyberpunk)
# This will be used as the initial theme when users first visit the site
NEXT_PUBLIC_DEFAULT_THEME=ocean

# Development Server Port (optional, Next.js defaults to 3000)
PORT=3000
