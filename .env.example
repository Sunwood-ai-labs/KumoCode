# Server Configuration
PORT=3000

# Articles Directory (optional, defaults to ./articles)
# ARTICLES_DIR=/path/to/articles

# Default Theme (optional, defaults to 'default')
# Available themes: default, ocean, sunset
# DEFAULT_THEME=default
