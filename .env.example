# Next.js Environment Variables

# Default Theme Color Scheme (available: ocean, default, sunset, cyberpunk)
# This determines the color palette (blues, purples, oranges, etc.)
NEXT_PUBLIC_DEFAULT_THEME=ocean

# Development Server Port (optional, Next.js defaults to 3000)
PORT=3000

# Note: Dark/Light mode is controlled by the theme toggle button
# The system will respect user's OS dark mode preference by default
